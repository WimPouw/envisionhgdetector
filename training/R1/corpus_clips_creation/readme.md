1. Install requirements in conda or local python environment
    ```bash
    conda create -n env_name python=3.12
    conda activate env_name
    pip install -r requirements.txt
    ```
2. update `corpus_details.yaml` as needed
2. Run main script
    ```bash
    python main.py
    ```
3. Your set output directory will have logs under `logs` folder.